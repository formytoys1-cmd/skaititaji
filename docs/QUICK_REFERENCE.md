# Памятка / Быстрый справочник — Skaitītāji

Короткая шпаргалка «под рукой»: ссылки, демо‑логины, как работает демо, консоль,
контроль деплоя. Секреты (API‑ключи, токены) сюда НЕ кладём — только публичное.

## Ссылки
- Сайт (прод): https://skaititaji.onrender.com
- Демо‑страница: https://skaititaji.onrender.com/demo
- Вход: https://skaititaji.onrender.com/login
- Помощь/мануалы: https://skaititaji.onrender.com/palidziba
- Репозиторий: https://github.com/formytoys1-cmd/skaititaji

## Демо‑логины (пароль у всех `demo1234`)
| Роль | E‑mail | Куда ведёт | Что показывает |
|---|---|---|---|
| Житель | `resident@demo.lv` | `/dzivoklis` | Подача показаний ХВС/ГВС, история, печать |
| Житель | `anna@demo.lv` | `/dzivoklis` | Другая квартира того же дома |
| Житель | `peteris@demo.lv` | `/dzivoklis` | Ещё одна квартира |
| Управляющий | `manager@demo.lv` | `/parvalde` | Дашборд, дома/квартиры/счётчики, синк с Visma |
| Админ/модератор | `admin@demo.lv` | `/admin` | Организации, типы счётчиков, **консоль обратной связи** |

## Как работает демо (важно)
- На `/demo` и лендинге кнопки «Войти в демо» ведут на **`/login?demo=<role>`**
  (`resident`/`manager`/`admin`). Открывается форма входа с **уже заполненными**
  e‑mail и паролем — остаётся нажать зелёную кнопку «Войти».
- **Почему не «в один клик»:** в проде вход‑обход `/demo-login` **отключён**
  (SEC‑001, вернёт 404), потому что он логинит без пароля в т.ч. как SUPERADMIN.
  Демо идёт через обычный вход реальными демо‑аккаунтами: хеш пароля, rate‑limit,
  CSRF. Это безопасно и одинаково работает в dev и в проде.
- В локальной разработке (`DEBUG=1`) `/demo-login` ещё доступен. Флаг —
  `ALLOW_DEMO_LOGIN` (в проде `0`).

## Консоль модератора (обратная связь агенту)
- Зайти под `admin@demo.lv` → **Konsole** (`/admin/inbox`).
- Пишете указание на доработку → «Отправить» → попадает агенту как задача.
- Автономный агент‑форк (ветка `agent/console`) слушает консоль и работает сам,
  мердж в `main` — по расписанию с гейтами (тесты/линт/безопасность).

## Контроль деплоя
- Хостинг: Render (autoDeploy с `main`). Прод не падает при неудачном деплое —
  Render держит старую версию, пока новая не пройдёт health‑check.
- Env для безопасности выставлены: `SECRETS_ENCRYPTION_KEY`, `ALLOW_DEMO_LOGIN=0`,
  `DEBUG=0`, `SECRET_KEY` (не дефолтный), `DATABASE_URL` (Postgres), `VISMA_MOCK=1`.
- Boot‑guard (`validate_production_config`) не даст стартовать проду с небезопасным
  конфигом.

## Приложения (PWA)
- Установка: открыть сайт в браузере телефона → «Добавить на главный экран».
- Работает офлайн (service worker), есть манифест и иконки. Нативные обёртки —
  на roadmap (см. `docs/night/`).

## Тесты и качество
- `pytest -q` — весь набор (unit/integration/security/contracts/compliance/infra).
- Линт: `ruff check .`. CI: `.github/workflows/tests.yml` (gate покрытия 80%).

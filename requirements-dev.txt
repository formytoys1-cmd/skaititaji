# Инструменты для тестирования и линтинга (НЕ входят в рантайм-образ прод).
# Устанавливать: pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.25.2
ruff==0.9.2
